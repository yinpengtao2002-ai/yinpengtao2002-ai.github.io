# 智谱 GLM API Key
# 在 https://open.bigmodel.cn/ 获取
GLM_API_KEY=

# GLM 模型选择（可选，默认 glm-4-flash）
# 可选: glm-4-flash (免费/便宜), glm-4-plus, glm-4
GLM_MODEL=glm-4-flash

# 设为 "true" 启用 GitHub Pages 静态导出（禁用 API 路由）
# 留空则为 Cloudflare Pages / Vercel 服务端模式
STATIC_EXPORT=
